# Block Review

- 2026-04-01: `python -m pytest` currently fails in `tests/test_harness_contract.py`
  because the repo `.gitignore` does not include `.local/`.
- The ideas/files route slice itself passes `tests/test_workspace_shell_contracts.py`
  and did not require shared shell edits.
