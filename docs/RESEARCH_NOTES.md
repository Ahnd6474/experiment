# Research Notes

- `ProjectsRoute.jsx` now renders a GitHub-like project hub using the seeded
  snapshot, `selectProjectBoard`, and `selectIntegrationOverview`.
- The full repo verification run exposed a harness expectation for `.local/`
  in `.gitignore`, so that entry was added as a small compatibility fix.
