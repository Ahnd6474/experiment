# Sample Target Repository

This tracked seed is copied into `.local/targets/sample-local` by
`scripts/materialize-sample-target.ps1`.

The content stays intentionally small so local experiments can start from a
known-good Git repository without depending on any external target checkout.
