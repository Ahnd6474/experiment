# Required for Codex or OpenAI-backed runs against real targets.
OPENAI_API_KEY=

# Required when the harness needs to clone or update a private GitHub target.
GITHUB_TOKEN=

# The tracked `sample-local` profile does not require any secrets.
